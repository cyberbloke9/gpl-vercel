# GPL MCP Server Environment Configuration
# Copy this file to .env and fill in your values

# Supabase Configuration
GPL_SUPABASE_URL=https://your-project.supabase.co
GPL_SUPABASE_SERVICE_KEY=your-service-role-key

# OAuth Configuration (for user-authenticated requests)
# These are automatically handled when using OAuth flow
# GPL_OAUTH_ACCESS_TOKEN=<set by OAuth flow>

# Optional: Restrict to specific scopes
# GPL_REQUIRED_SCOPES=gpl:read,gpl:write
